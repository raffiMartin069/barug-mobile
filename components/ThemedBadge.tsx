import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const ThemedBadge = ({label, ...props}) => {
  return (
    <View>
      <Text>ThemedBadge</Text>
    </View>
  )
}

export default ThemedBadge

const styles = StyleSheet.create({})